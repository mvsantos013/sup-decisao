{
  "name": "backend",
  "version": "1.0.0",
  "description": "```\r pipenv shell\r pipenv install\r npm install\r npm install -g serverless\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless-python-requirements": "^5.4.0"
  }
}
